双向绑定:将页面元素数值,在JS中赋值,页面改变JS中值改变,JS中值改变,页面也会重新渲染改变.$scope.$apply();脏值检查

特别是在页面需要回显信息的时候,双向绑定会带来极大的便利,在调用接口得到回参之后,直接将回参对应的变量赋值,apply后,页面自动就会
将新值渲染上去.对于普通的div span 用ng-bing(单向) ng-model(双向)的指令就能解决;对于现公司自行开发的组件,就显得比较困难了,它会自行解析,生成的ID CLASS都会变化
需要特殊处理,用JQUERY来查找或者用组件自带的函数.
